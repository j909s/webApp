<!-- <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>users List</title>
    </head>

    <body>
        <h1>users List</h1>
        <ul>
            <% users.forEach(user=> { %>
                <li><a href="/user/<%= user._id %>">
                        <%= user.name %> 
                    </a></li>
                <% }) %>
        </ul>
        <a href="/user/new">Add a New user</a>
    </body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>user List</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        /* Forest green theme */
        :root {
            --forest-green: #2F4F4F;  /* Dark slate gray - forest green shade */
            --forest-green-light: #3D6B6B;  /* Lighter variant */
            --forest-green-dark: #223838;   /* Darker variant */
        }

        body {
            background-color: #F5F6F0; /* Light cream background for contrast */
            color: var(--forest-green-dark);
        }

        h1 {
            color: var(--forest-green-dark);
        }

        .btn-primary {
            background-color: var(--forest-green);
            border-color: var(--forest-green);
        }

        .btn-primary:hover {
            background-color: var(--forest-green-dark);
            border-color: var(--forest-green-dark);
        }

        .list-group-item {
            background-color: #FFFFFF;
            border-color: var(--forest-green-light);
            color: var(--forest-green-dark);
        }

        .list-group-item:hover {
            background-color: #F5F6F0;
        }

        a.text-decoration-none {
            color: var(--forest-green);
        }

        a.text-decoration-none:hover {
            color: var(--forest-green-dark);
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Users List</h1>
        <ul class="list-group mb-4">
            <% users.forEach(user => { %>
                <li class="list-group-item">
                    <a href="/user/<%= user._id %>" class="text-decoration-none">
                        <%= user.name %>
                    </a>
                </li>
            <% }) %>
        </ul>
        <a href="/user/new" class="btn btn-primary">Add a user</a>
    </div>

    <!-- Bootstrap JS Bundle (optional, if you need Bootstrap's JavaScript features) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>